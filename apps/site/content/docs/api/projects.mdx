---
title: Projects
full: true
_openapi:
  preload:
    - ./public/openapi.json
  toc:
    - depth: 2
      title: List Projects
      url: '#list-projects'
    - depth: 2
      title: Create Project
      url: '#create-project'
    - depth: 2
      title: Get Project
      url: '#get-project'
    - depth: 2
      title: Delete Project
      url: '#delete-project'
    - depth: 2
      title: Update Project
      url: '#update-project'
    - depth: 2
      title: List Project Backups
      url: '#list-project-backups'
    - depth: 2
      title: Update Project Sharing
      url: '#update-project-sharing'
    - depth: 2
      title: Transfer Project
      url: '#transfer-project'
  structuredData:
    headings:
      - content: List Projects
        id: list-projects
      - content: Create Project
        id: create-project
      - content: Get Project
        id: get-project
      - content: Delete Project
        id: delete-project
      - content: Update Project
        id: update-project
      - content: List Project Backups
        id: list-project-backups
      - content: Update Project Sharing
        id: update-project-sharing
      - content: Transfer Project
        id: transfer-project
    contents:
      - content: Session-gate UNVERIFIED (id-less mutating route, skipped for safety —
          see apidocSkipFullScopeReason). Scope is confirmed — every route is
          probed at zero scope — but whether a personal access token is
          additionally rejected here by a session-only gate is not; both
          security alternatives below are listed, only one is guaranteed
          correct.
        heading: create-project
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: get-project
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: delete-project
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: update-project
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: list-project-backups
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: update-project-sharing
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: transfer-project
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./public/openapi.json" webhooks={[]} operations={[{"path":"/api/projects","method":"get"},{"path":"/api/projects","method":"post"},{"path":"/api/projects/{projectId}","method":"get"},{"path":"/api/projects/{projectId}","method":"delete"},{"path":"/api/projects/{projectId}","method":"put"},{"path":"/api/projects/{projectId}/backups","method":"get"},{"path":"/api/projects/{projectId}/sharing","method":"put"},{"path":"/api/projects/{projectId}/transfer","method":"put"}]} showTitle />
    </>
  );
}