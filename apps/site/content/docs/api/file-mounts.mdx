---
title: File Mounts
full: true
_openapi:
  preload:
    - ./public/openapi.json
  toc:
    - depth: 2
      title: List File Mounts
      url: '#list-file-mounts'
    - depth: 2
      title: Create File Mount
      url: '#create-file-mount'
    - depth: 2
      title: Delete File Mount
      url: '#delete-file-mount'
    - depth: 2
      title: Update File Mount
      url: '#update-file-mount'
    - depth: 2
      title: Reveal File Mount
      url: '#reveal-file-mount'
  structuredData:
    headings:
      - content: List File Mounts
        id: list-file-mounts
      - content: Create File Mount
        id: create-file-mount
      - content: Delete File Mount
        id: delete-file-mount
      - content: Update File Mount
        id: update-file-mount
      - content: Reveal File Mount
        id: reveal-file-mount
    contents:
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: list-file-mounts
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: create-file-mount
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: delete-file-mount
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: update-file-mount
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: reveal-file-mount
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./public/openapi.json" webhooks={[]} operations={[{"path":"/api/projects/{projectId}/applications/{applicationId}/file-mounts","method":"get"},{"path":"/api/projects/{projectId}/applications/{applicationId}/file-mounts","method":"post"},{"path":"/api/projects/{projectId}/applications/{applicationId}/file-mounts/{fileMountId}","method":"delete"},{"path":"/api/projects/{projectId}/applications/{applicationId}/file-mounts/{fileMountId}","method":"put"},{"path":"/api/projects/{projectId}/applications/{applicationId}/file-mounts/{fileMountId}/reveal","method":"get"}]} showTitle />
    </>
  );
}