---
title: Application Volumes & Backups
full: true
_openapi:
  preload:
    - ./public/openapi.json
  toc:
    - depth: 2
      title: List Docker Volumes
      url: '#list-docker-volumes'
    - depth: 2
      title: List Application Volumes
      url: '#list-application-volumes'
    - depth: 2
      title: Create Application Volume
      url: '#create-application-volume'
    - depth: 2
      title: Delete Application Volume
      url: '#delete-application-volume'
    - depth: 2
      title: List Volume Backup Configs
      url: '#list-volume-backup-configs'
    - depth: 2
      title: Create Volume Backup Config
      url: '#create-volume-backup-config'
    - depth: 2
      title: Delete Volume Backup Config
      url: '#delete-volume-backup-config'
    - depth: 2
      title: Update Volume Backup Config
      url: '#update-volume-backup-config'
    - depth: 2
      title: Run Volume Backup Config
      url: '#run-volume-backup-config'
    - depth: 2
      title: List Volume Backups
      url: '#list-volume-backups'
    - depth: 2
      title: Restore Volume Backup
      url: '#restore-volume-backup'
    - depth: 2
      title: List Volume Restores
      url: '#list-volume-restores'
  structuredData:
    headings:
      - content: List Docker Volumes
        id: list-docker-volumes
      - content: List Application Volumes
        id: list-application-volumes
      - content: Create Application Volume
        id: create-application-volume
      - content: Delete Application Volume
        id: delete-application-volume
      - content: List Volume Backup Configs
        id: list-volume-backup-configs
      - content: Create Volume Backup Config
        id: create-volume-backup-config
      - content: Delete Volume Backup Config
        id: delete-volume-backup-config
      - content: Update Volume Backup Config
        id: update-volume-backup-config
      - content: Run Volume Backup Config
        id: run-volume-backup-config
      - content: List Volume Backups
        id: list-volume-backups
      - content: Restore Volume Backup
        id: restore-volume-backup
      - content: List Volume Restores
        id: list-volume-restores
    contents:
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: list-application-volumes
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: create-application-volume
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: delete-application-volume
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: list-volume-backup-configs
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: create-volume-backup-config
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: delete-volume-backup-config
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: update-volume-backup-config
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: run-volume-backup-config
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: list-volume-backups
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: restore-volume-backup
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: list-volume-restores
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./public/openapi.json" webhooks={[]} operations={[{"path":"/api/docker/volumes","method":"get"},{"path":"/api/projects/{projectId}/applications/{applicationId}/volumes","method":"get"},{"path":"/api/projects/{projectId}/applications/{applicationId}/volumes","method":"post"},{"path":"/api/projects/{projectId}/applications/{applicationId}/volumes/{volumeId}","method":"delete"},{"path":"/api/projects/{projectId}/applications/{applicationId}/volumes/{volumeId}/backup-configs","method":"get"},{"path":"/api/projects/{projectId}/applications/{applicationId}/volumes/{volumeId}/backup-configs","method":"post"},{"path":"/api/projects/{projectId}/applications/{applicationId}/volumes/{volumeId}/backup-configs/{configId}","method":"delete"},{"path":"/api/projects/{projectId}/applications/{applicationId}/volumes/{volumeId}/backup-configs/{configId}","method":"put"},{"path":"/api/projects/{projectId}/applications/{applicationId}/volumes/{volumeId}/backup-configs/{configId}/run","method":"post"},{"path":"/api/projects/{projectId}/applications/{applicationId}/volumes/{volumeId}/backups","method":"get"},{"path":"/api/projects/{projectId}/applications/{applicationId}/volumes/{volumeId}/backups/{backupId}/restore","method":"post"},{"path":"/api/projects/{projectId}/applications/{applicationId}/volumes/{volumeId}/restores","method":"get"}]} showTitle />
    </>
  );
}