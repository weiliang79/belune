---
title: Deployments
full: true
_openapi:
  preload:
    - ./public/openapi.json
  toc:
    - depth: 2
      title: Get Global Deployments
      url: '#get-global-deployments'
    - depth: 2
      title: List Deployments
      url: '#list-deployments'
    - depth: 2
      title: Get Deployment
      url: '#get-deployment'
    - depth: 2
      title: Stream Build Logs
      url: '#stream-build-logs'
  structuredData:
    headings:
      - content: Get Global Deployments
        id: get-global-deployments
      - content: List Deployments
        id: list-deployments
      - content: Get Deployment
        id: get-deployment
      - content: Stream Build Logs
        id: stream-build-logs
    contents:
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: list-deployments
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: get-deployment
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope. Session-gate UNVERIFIED
          (streaming (SSE) route — the connection never closes, so a bounded
          probe can't observe it). Scope is confirmed — every route is probed at
          zero scope — but whether a personal access token is additionally
          rejected here by a session-only gate is not; both security
          alternatives below are listed, only one is guaranteed correct.
        heading: stream-build-logs
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./public/openapi.json" webhooks={[]} operations={[{"path":"/api/deployments","method":"get"},{"path":"/api/projects/{projectId}/applications/{applicationId}/deployments","method":"get"},{"path":"/api/projects/{projectId}/applications/{applicationId}/deployments/{deploymentId}","method":"get"},{"path":"/api/projects/{projectId}/applications/{applicationId}/deployments/{deploymentId}/build-logs","method":"get"}]} showTitle />
    </>
  );
}