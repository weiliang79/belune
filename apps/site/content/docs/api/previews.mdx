---
title: Preview Environments
full: true
_openapi:
  preload:
    - ./public/openapi.json
  toc:
    - depth: 2
      title: List Previews
      url: '#list-previews'
    - depth: 2
      title: Update Preview Config
      url: '#update-preview-config'
    - depth: 2
      title: Delete Preview
      url: '#delete-preview'
  structuredData:
    headings:
      - content: List Previews
        id: list-previews
      - content: Update Preview Config
        id: update-preview-config
      - content: Delete Preview
        id: delete-preview
    contents:
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: list-previews
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: update-preview-config
      - content: Scoped to one project — a token pinned to a different project is
          rejected outside it, regardless of scope.
        heading: delete-preview
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./public/openapi.json" webhooks={[]} operations={[{"path":"/api/projects/{projectId}/applications/{applicationId}/previews","method":"get"},{"path":"/api/projects/{projectId}/applications/{applicationId}/previews/config","method":"put"},{"path":"/api/projects/{projectId}/applications/{applicationId}/previews/{previewId}","method":"delete"}]} showTitle />
    </>
  );
}