---
title: Admin — Users & Invitations
description: 9 probed endpoints.
---

{/* DO NOT EDIT — generated by TestGenerateAPIReference
     (apps/api/internal/handler/apidoc_generate_test.go).
     Regenerate with `task generate:api-docs`. */}

| Method | Path | Handler | Scope | Notes |
|---|---|---|---|---|
| GET | `/api/users` | `ListUsers` | `read` | admin role required |
| POST | `/api/users` | `CreateUser` | `write` | admin role required; session gate not probed (id-less mutating route, skipped for safety — see apidocSkipFullScopeReason) |
| GET | `/api/users/invitations` | `ListPendingInvitations` | `read` | admin role required |
| DELETE | `/api/users/invitations/{invitationId}` | `RevokeInvitation` | `write` | admin role required |
| POST | `/api/users/invite` | `InviteUser` | `write` | admin role required; session gate not probed (id-less mutating route, skipped for safety — see apidocSkipFullScopeReason) |
| DELETE | `/api/users/{userId}` | `DeleteUser` | `write` | admin role required; **session required — rejects a PAT regardless of scope** |
| PUT | `/api/users/{userId}/password` | `ResetUserPassword` | `write` | admin role required; **session required — rejects a PAT regardless of scope** |
| PUT | `/api/users/{userId}/role` | `UpdateUserRole` | `write` | admin role required; **session required — rejects a PAT regardless of scope** |
| POST | `/api/users/{userId}/totp/reset` | `AdminResetUserTOTP` | `write` | admin role required; **session required — rejects a PAT regardless of scope** |
